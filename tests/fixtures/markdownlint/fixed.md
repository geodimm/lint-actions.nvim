# Playground

A line with trailing spaces.

Visit <https://example.test> for more, and see this deliberately long paragraph line that runs past the default line length limit markdownlint enforces.

* Loose bullet
No newline at the end.
